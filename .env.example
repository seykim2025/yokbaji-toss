# Backend API base URL (yokbaji-engine)
VITE_API_URL=https://yokbaji-engine.vercel.app

# Set to "true" to seed two default characters on first load in non-dev builds.
# DEV builds always seed regardless of this flag.
VITE_SEED_DEFAULTS=false
